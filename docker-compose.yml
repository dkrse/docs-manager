version: "3.8"

services:
  app:
    build: .
    container_name: document-manager
    ports:
      - "5214:5214"
    environment:
      - DATABASE_URL=postgresql://postgres:dexxxx1@192.168.1.189/documents
      - SECRET_KEY=a7f3b9c1d4e8f2a5b6c7d8e9f0a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8
      - UPLOAD_DIR=/opt/apps/document-manager/uploads
    volumes:
      - doc-uploads:/opt/apps/document-manager/uploads
    restart: unless-stopped

volumes:
  doc-uploads:
